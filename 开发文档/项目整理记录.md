# 项目整理记录

## 2026-08-14

### 正式项目边界

`H:\CODE\CS_bot` 只保留正式仓库 `CS2_INVENTORY`。仓库内目录职责如下：

- `src/cs2_inventory/`：Web、认证、数据库、库存算法、监控及后台任务。
- `tests/`：库存算法与平台功能测试。
- `deploy/`：Apache、systemd、发布及回滚配置。
- `scripts/`：打包、初始化数据、本地化及 HealthDoc 备份维护脚本。
- `migrations/`：数据库迁移。
- `开发文档/`：开发与运维文档。

### 合并

- `tests/test_legacy_complete.py` 和 `tests/test_legacy_query.py` 的用例已经由 `tests/test_inventory_engine.py` 完整覆盖；重复文件已合并删除。
- 合并前共执行 67 项测试，其中 19 项为重复用例；合并后保留 48 项互不重复的测试，覆盖范围不变。
- 旧版查询器、逆向分析和平台调研资料不再作为正式运行时依赖；正式实现统一位于 `src/cs2_inventory/inventory_engine.py`。

### 清理

- 删除仓库内测试状态目录、Python 缓存、Ruff 缓存、发布包及旧验证产物。
- 将 `CS_bot` 根目录下的旧工程、实验环境、逆向分析、回滚副本、临时脚本和临时数据迁出到独立隔离目录。
- Git 继续忽略数据库、密钥、真实库存响应、测试状态和发布归档。

### 恢复

隔离目录中的 `MANIFEST.tsv` 记录原始路径、大小和迁移后路径；`ROLLBACK.ps1` 可把隔离内容恢复到整理前的位置。正式仓库的代码回滚由仓库根目录的 `ROLLBACK.sh` 验证。
