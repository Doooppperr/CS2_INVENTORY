# 架构基线

库存引擎从原始单次查询项目迁入 `src/cs2_inventory/inventory_engine.py`。账号、持久化、任务调度和 Web 界面在后续提交中构建，库存引擎的多来源最大覆盖行为由迁移后的回归测试保护。

应用启动只在全新数据库中执行一次默认账号初始化，并用系统状态标记防止 Web、Worker 和定时 CLI 重复播种。管理员控制台按概览、用户管理、全部目标三个按需加载的子页面组织；列表导航状态保存在 URL，详情页不持有易丢失的内存页码。
